[pytest]
markers =
    ui: testes de interface (Playwright)
    smoke: subconjunto rápido de testes críticos

addopts = -v --junitxml=results.xml --html=reports/report.html --self-contained-html
testpaths = tests